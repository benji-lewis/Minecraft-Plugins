name: Transport

on:
  push:
    branches: ["main", "develop"]
    paths: ["Transport/**", "docs/MC-Docs/docs/plugins/transport.md", ".github/workflows/transport.yml"]
  pull_request:
    paths: ["Transport/**", "docs/MC-Docs/docs/plugins/transport.md", ".github/workflows/transport.yml"]

jobs:
  test-transport:
    runs-on: ubuntu-latest
    env:
      PLUGIN_VERSION: 1.0.${{ github.run_number }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Run tests
        run: mvn -f Transport/pom.xml test
      - name: Set build version
        run: mvn -f Transport/pom.xml versions:set -DnewVersion=${PLUGIN_VERSION} -DgenerateBackupPoms=false
      - name: Package jar
        run: mvn -f Transport/pom.xml -DskipTests package
      - name: Upload jar artifact
        uses: actions/upload-artifact@v4
        with:
          name: transport-jar
          path: Transport/target/transport-${{ env.PLUGIN_VERSION }}.jar

  deploy-testing:
    runs-on: ubuntu-latest
    needs: test-transport
    if: github.event_name == 'pull_request'
    environment: testing
    env:
      PLUGIN_VERSION: 1.0.${{ github.run_number }}
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: transport-jar
          path: dist
      - name: Remove previous deployment jar
        run: rm -f dist/transport.jar
      - name: Upload jar to testing server
        uses: Dylan700/sftp-upload-action@latest
        with:
          server: ${{ vars.SFTP_URL }}
          username: ${{ vars.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          port: ${{ vars.SFTP_PORT }}
          uploads: ./dist/transport-${{ env.PLUGIN_VERSION }}.jar => ./plugins/transport.jar

  deploy-production:
    runs-on: ubuntu-latest
    needs: test-transport
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment: production
    env:
      PLUGIN_VERSION: 1.0.${{ github.run_number }}
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: transport-jar
          path: dist
      - name: Remove previous deployment jar
        run: rm -f dist/transport.jar
      - name: Upload jar to production server
        uses: Dylan700/sftp-upload-action@latest
        with:
          server: ${{ vars.SFTP_URL }}
          username: ${{ vars.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          port: ${{ vars.SFTP_PORT }}
          uploads: ./dist/transport-${{ env.PLUGIN_VERSION }}.jar => ./plugins/transport.jar
