name: Kim Jong Un 3

on:
  push:
    branches: ["main", "develop"]
    paths: ["kim-jong-un-3/**"]
  pull_request:
    paths: ["kim-jong-un-3/**"]

jobs:
  test-kim-jong-un-3:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle
      - name: Run tests
        run: gradle -p kim-jong-un-3 test --no-daemon
      - name: Package addon jar
        run: gradle -p kim-jong-un-3 addonJar -PoutDir=kim-jong-un-3/build/addons --no-daemon
      - name: Upload addon artifact
        uses: actions/upload-artifact@v4
        with:
          name: kim-jong-un-3-addon
          path: kim-jong-un-3/build/libs/kim-jong-un-3-1.0.0.jar

  deploy-testing:
    runs-on: ubuntu-latest
    needs: test-kim-jong-un-3
    if: github.event_name == 'pull_request'
    environment: Testing Server
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: kim-jong-un-3-addon
          path: dist
      - name: Upload addon to testing server
        uses: Dylan700/sftp-upload-action@latest
        with:
          server: ${{ vars.SFTP_URL }}
          username: ${{ vars.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          port: ${{ vars.SFTP_PORT }}
          uploads: ./dist/kim-jong-un-3-1.0.0.jar => ./plugins/

  deploy-production:
    runs-on: ubuntu-latest
    needs: test-kim-jong-un-3
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment: Production
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: kim-jong-un-3-addon
          path: dist
      - name: Upload addon to production server
        uses: Dylan700/sftp-upload-action@latest
        with:
          server: ${{ vars.SFTP_URL }}
          username: ${{ vars.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          port: ${{ vars.SFTP_PORT }}
          uploads: ./dist/kim-jong-un-3-1.0.0.jar => ./plugins/
